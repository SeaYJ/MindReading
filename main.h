#ifndef _MAIN_H
#define _MAIN_H

#include <stdio.h>
#include <stdlib.h>

bool login(const char* username, const char* password);
bool regAccount(const char* username, const char* password);

#endif